# Server
PORT=4000
# IMPORTANT: Set a strong random secret in production (e.g. openssl rand -hex 32)
SESSION_SECRET=change_me_in_production
NODE_ENV=development

# Database
DATABASE_URL=file:./data/gitrip.sqlite

# External services (keep these secret — never commit real keys)
ORS_API_KEY=your_openrouteservice_key_here
GOOGLE_DIRECTIONS_API_KEY=your_google_directions_key_here
NOMINATIM_EMAIL=your_email@example.com
NEWS_API_KEY=your_gnews_api_key_here

# CORS / URLs
BASE_URL=http://localhost:4000
CORS_ORIGIN=http://localhost:4000